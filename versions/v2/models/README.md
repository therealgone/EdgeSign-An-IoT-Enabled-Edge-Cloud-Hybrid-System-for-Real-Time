# Models Directory

Copy your trained model here:

```
models/
├── sign_language_model.pkl
└── sign_language_model_lite.pkl   # optional lightweight variant
```

Train with V1 (`versions/v1/train_model.py`) or copy from a previous build.
