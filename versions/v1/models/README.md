# Models Directory

Place your trained model here after running `train_model.py`:

```
models/
└── sign_language_model.pkl
```

Model files are gitignored. Train locally with V1 or copy an existing `.pkl` file into this folder.
