# Models Directory

Required for V3 to run:

```
models/
├── sign_language_model.pkl        # required
└── sign_language_model_lite.pkl   # optional
```

Train with V1 or copy an existing model. Files are gitignored — add them locally before running.
